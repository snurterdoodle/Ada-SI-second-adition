# Required: auth key for LiteLLM proxy (use any secure string)
LITELLM_MASTER_KEY=sk-ada-dev-key

# Add keys for the providers you want to use.
# Only providers with valid keys will return models in the picker.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
